{
  "name": "handoff",
  "version": "0.4.0",
  "description": "Lets the agent see session token usage, and at 150k/200k/250k... auto-injects a reminder to write a forward-looking handoff file and the Stop hook then schedules /compact so the session continues from the handoff file."
}
